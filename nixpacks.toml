[providers]
node = {}
python = {}

[phases.setup]
nixPkgs = ['...', 'nodejs', 'python311', 'ffmpeg']

[phases.install]
cmds = [
    'npm install',
    'pip install -r python/requirements.txt'
]

[start]
cmd = 'bash start.sh'
